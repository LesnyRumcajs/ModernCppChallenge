#pragma once

namespace cppchallenge::math {
    /**
     * Calculates the sum of all natural numbers up to the specified limit that are divisable by either 3 or 5
     * @param limit
     * @return sum
     */
    unsigned long sum_of_naturals_divisable(unsigned limit);
}
